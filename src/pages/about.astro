---
import BaseLayout from '../layouts/BaseLayout.astro';
import { profile } from '../data/profile';
---

<BaseLayout title="About" description="关于 YZH。" path="/about">
  <section class="shell page-header">
    <div>
      <p class="eyebrow">About</p>
      <h1>关于我</h1>
    </div>
    <p>这一页不重复简历，而是解释我关注什么、怎样做项目，以及希望通过这个网站留下什么。</p>
  </section>

  <section class="section shell about-grid">
    <article class="statement">
      <h2>你好，我是 {profile.name}。</h2>
      <p>{profile.intro}</p>
      <p>
        我喜欢把模糊的问题整理成可执行的结构：先理解真实材料和使用流程，再选择足够简单的工程方案完成实现与验证。
      </p>
      <p>
        这里的项目、文章和笔记会随着实践持续变化。相比一份固定介绍，我更希望它成为能够看见成长过程的公开档案。
      </p>
    </article>

    <aside class="info-panel">
      <div class="fact">
        <span>Location</span>
        <strong>{profile.location}</strong>
      </div>
      <div class="fact">
        <span>Focus</span>
        <strong>Software Engineering</strong>
      </div>
      <div class="fact">
        <span>Status</span>
        <strong>Open to collaboration</strong>
      </div>
      <a class="button-link" href={`mailto:${profile.email}`}>发送邮件</a>
    </aside>
  </section>
</BaseLayout>

<style>
  .about-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(260px, 340px);
    gap: clamp(48px, 8vw, 96px);
  }

  .statement {
    max-width: 720px;
  }

  .statement h2 {
    margin: 0 0 24px;
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    font-weight: 700;
    line-height: 1.2;
    letter-spacing: -0.02em;
  }

  .statement > p {
    color: var(--text-secondary);
    font-size: 1.05rem;
    line-height: 1.9;
  }

  .info-panel {
    align-self: start;
    padding: 24px;
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    background: var(--bg-subtle);
  }

  .fact {
    display: grid;
    gap: 6px;
    padding-block: 16px;
    border-bottom: 1px solid var(--border);
  }

  .fact:last-of-type {
    border-bottom: none;
  }

  .fact span {
    color: var(--text-tertiary);
    font-family: var(--mono);
    font-size: 0.68rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .fact strong {
    font-size: 0.95rem;
  }

  .info-panel .button-link {
    margin-top: 20px;
    width: 100%;
  }

  @media (max-width: 760px) {
    .about-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
